namespace HudVariables
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /* INT initializations */ int score = 0; int health = 0;  int damage = 0; int enenmyHealth = 0;
            /* String initializations */ string studioName = "NameWasTakenStudios";
            /* Float initializations */float scoreMult = 1.00f; float lives = 3.00f;

            Console.WriteLine("Score: " + score + " Health: " + health + " Lives: " + lives);

            // score = score + enemyValue ||  score+= enemyValue;
            
        }
    }
}